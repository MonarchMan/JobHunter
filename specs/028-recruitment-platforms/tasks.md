# 028 招聘平台来源任务

> 状态：In Progress

- [x] **PLT-T021**：调研猎聘当前官网与 BossHunter，实现 HTTP 优先的一批列表／一条详情最小闭环，核验身份正文、隔离正式入库和统一职位查询。覆盖 PLT-004、PLT-005、PLT-008、PLT-009、PLT-010、PLT-014、PLT-015、PLT-016、PLT-018。
- [x] **PLT-T022**：在猎聘最小闭环基础上验证持续批次协议与必要请求头，完成生产 CDP 提供方、CLI／Web 装配及有限分页／整批验收；不得以研究注入连接器或单详情保存代替生产入口验收。本项限学生首页、首批整批入库与同会话有效末批；不代表请求头逐项最小性、内置浏览器兼容或长期稳定。覆盖 PLT-004、PLT-007、PLT-008、PLT-009、PLT-014、PLT-015、PLT-018。

2026-09-20 后续 lint 修复：下文记录的旧 `platform-boss.spec.ts` 13 项问题已清除。响应与请求 JSON 使用 Zod 边界校验，移除非空断言，void 回调改为块体；敏感值检查仍针对完整原始响应，不因 Schema 投影而缩小范围。该文件 ESLint、格式、Web 类型检查和 2 项本地浏览器测试通过；未访问真实 BOSS，未改变生产行为。

- [x] **PLT-T019**：按 ADR-0030 统一批次自动补齐入库与职位页来源筛选；覆盖串行、部分失败、取消／旧代次、查询计数分页隔离及无点击保存交互。仅运行定向离线与本地浏览器验证，不触发新的平台批量请求。覆盖 PLT-009、PLT-011、PLT-014、PLT-018。

2026-09-20 统一职位列表：每次仅处理一页，后台串行补齐本页详情后逐条入库，不自动翻页；失败／取消保留已提交职位。来源页保留连接和获取动作，删除摘要职位卡片；职位页默认官网，可切换平台及 provider，平台默认包含未知招聘类别，同名公司在选定来源范围内筛选而不合并身份。复用 SelectField、职位表格、移动卡片与分页，没有修改视觉令牌。7 项应用单元、2 项智联 Worker 单元、3 项 SQLite 集成、5 项 Web／CLI／生产 Worker 离线链路及 7 项本地浏览器测试通过；定向类型检查、实现文件 ESLint、Web 生产构建、文档检查和 strict UI audit 通过。扩展 ESLint 检查发现旧 platform-boss.spec.ts 已有 13 项 any／非空断言等问题（原行未改），不夹带无关重构。未请求真实平台、未重启已有 Worker／CDP、未迁移或清理生产数据。新批次自动补齐需在下次部署新版 Worker 后生效；历史成功批次不补抓。旧在线 BOSS smoke 增加 `BOSS_SMOKE_AUTO_BATCH=1` 显式开关，避免旧授权环境意外触发整页详情请求；长期稳定性门禁仍未完成。

- [x] **PLT-T018**：前程无忧（51job）按官网辅助模式对齐列表／正文协议，完成最小 HTTP 及隔离生产 Worker／CLI／Web 首末页闭环；每次最多两至三批，间隔至少 5 秒，异常即停止。该项完成不表示纯 HTTP 自动翻页或长期稳定。覆盖 PLT-004、PLT-005、PLT-008、PLT-009、PLT-010、PLT-014、PLT-015、PLT-016。

- [x] **PLT-T016**：工程化智联主站搜索会话、实时详情和 CDP 双协议选择，复用 CLI／Worker／Web；覆盖模板白名单、身份核验、筛选保留、分页边界、5 秒节流、取消及失败冻结，完成隔离单批／详情真实链路。覆盖 PLT-004、PLT-005、PLT-007、PLT-008、PLT-009、PLT-010、PLT-014、PLT-015、PLT-016。
- [ ] **PLT-T017**：复核智联主站生产会话连续两批请求，保留本轮第二批 network_error 证据；先前独立 HTTP 首页／第二页／末页成功不代替生产装配多批验收。覆盖 PLT-008、PLT-015。

- [x] **PLT-T015**：定位并修复智联原失败详情的段落／有序列表解析，保持身份校验，覆盖合法排版、畸形结构、未知标签与实体；对原职位实时获取、隔离入库和查询复核。覆盖 PLT-009、PLT-014、PLT-015。
- [x] **PLT-T014**：以已授权请求模板装配智联推荐 HTTP 会话，在同次真实 Worker 运行内完成推荐、选取、实时详情、隔离正式入库与统一查询；覆盖 PLT-004、PLT-005、PLT-008、PLT-009、PLT-014、PLT-016。记录失败样本，不等同于所有详情、分页或生产 UI 已通过。
- [x] **PLT-T013**：实现智联校园单条已展示职位的受限 HTTP 详情会话，核对稳定身份、正文与公司名称，经通用 Worker 在隔离库保存和查询；覆盖 PLT-003、PLT-005、PLT-009、PLT-014、PLT-016。不代表推荐分页或生产装配完成。
- [x] **PLT-T012**：先基于已授权智联页面验证一批列表和一条详情的最小 HTTP 闭环，再装配 CLI／Worker／Web／正式入库；按校园推荐与主站搜索分别记录能力，不因共享域名混用协议。覆盖 PLT-004、PLT-008、PLT-010、PLT-014、PLT-015、PLT-016。
- [x] **PLT-T010**：补齐 provider 绑定仓储／任务／Web 投影隔离，覆盖同名同 ID、断开、失败与跨平台任务拒绝；不注册未实现连接器。覆盖 PLT-016。
- [x] **PLT-T011**：可见详情 POST 记录交互并真实展示清理状态，覆盖隐藏／预取／失败／CSRF／官网不变与清理保护。覆盖 PLT-017。
- [x] **PLT-T001**：确定专题架构、统一入库与保留策略，新增 ADR 并同步总体／数据／Worker 文档。覆盖 PLT-001、PLT-002、PLT-011、PLT-012、PLT-014。
- [ ] **PLT-T002**：完成 BOSS 参数对应、业务码 37、必要会话字段和错误分类的限量研究；单独登记各能力证据。覆盖 PLT-004、PLT-006、PLT-008、PLT-010、PLT-015。
- [x] **PLT-T003**：新增平台契约与实现包，建立允许域名传输、内存会话、代次和脱敏诊断。覆盖 PLT-004、PLT-005、PLT-006、PLT-014。
- [ ] **PLT-T004**：迁移来源类型、公司身份、连接与观察归属，抽出正式职位共享写入；验证官网 ID、种子、查询及缺失行为不变。覆盖 PLT-001、PLT-002、PLT-003、PLT-011、PLT-014。
- [x] **PLT-T005**：实现推荐一批、翻页、详情与自动入库短任务，覆盖重复、取消、重启、失效代次和部分成功。覆盖 PLT-007、PLT-008、PLT-009、PLT-010。
- [x] **PLT-T006**：实现来源类别保留策略、dry-run、显式启用、引用保护和事务内重检，接入现有维护而非新增缓存清理。覆盖 PLT-012、PLT-013。
- [x] **PLT-T007**：实现连接和浏览入口，区分摘要／已保存／核验状态，复用正式职位详情与匹配；实现前按 UI 规范补交互设计。覆盖 PLT-001、PLT-006、PLT-007、PLT-009、PLT-011。
- [ ] **PLT-T008**：执行离线契约、迁移、SQLite、任务与清理回归；显式限量 smoke 通过后按能力晋级，再独立验证智联。覆盖 PLT-003、PLT-005、PLT-008、PLT-010、PLT-013、PLT-015。

## 本轮记录

2026-09-20 用户允许外部 Chrome 替代路径后，原生 CDP 连接在不启用 Runtime／Console／Debugger 的情况下保持页面正常；生产连接器通过同一已授权原生 Socket 执行真实 attach／Network／逐目标 Cookie 命令，不再使用内置浏览器虚拟 target 映射。仅正常点击一次“最新”取得推荐模板，随后列表和详情均为独立 HTTP，无刷新、自动重连、环境伪造或官网检测修改。该对照证明替代路径可用，不足以将内置浏览器白屏唯一归因于某个 CDP 命令或子探针。

真实生产验收通过：CLI connect 子进程、生产 Worker 初始化及 WebPlatformService 发布的 next 任务成功；列表原始 39 条，有效 24 条，15 条已知缺少公司 ID 形态排除计数。24 条详情均完成身份与正文核验并逐条入隔离 SQLite，未使用研究单条选择包装器。随后直接调用同一生产会话 readNext 验证 UP，返回 candidates=0、skipped=0、hasMore=false 的有效末批，停止第三批；该续批不是第二个 CLI／Web 队列任务，也不是两个非空批次的证明。26 次独立 HTTP（两次列表、24 次详情）均 HTTP 200，最小起始间隔 5000ms。

Web 应用快照 savedCount=24，职位页使用的 WebJobQueryService 在 platform／liepin 范围返回 24，默认官网范围返回 0；24 条正文完整，最短 216 字符。数据库 jobs=24、sync_runs=0、missing_count 合计=0、外键错误=0。证据库为 `/var/folders/mf/l52yxkf12jx0ycrpdm83k7b40000gn/T/jobhunter-liepin-unified-live-TnP9XT/jobhunter.sqlite`，未写用户业务库；本次没有操作线上 Web UI，其交互仍以此前本地浏览器回归为证。Worker 已退出、原授权 Socket 保留空闲，本轮响应原文内存已释放。T022 在上述有限能力内完成；内置浏览器白屏、请求头逐项最小性、多轮非空分页和长期稳定不据此标已解决，12 小时／每三小时的联合观察继续暂停。

收尾重新运行 44 项猎聘／CDP／生产 CLI-Worker-SQLite 定向回归，全部通过；文档格式、一致性和 diff 检查通过。没有运行全量门禁或新增平台 HTTP 请求。

2026-09-20 白屏进一步诊断：页面实际加载的 `https://concat.lietou-static.com/fe-lib-pc/v6/security/1.0.0/security.min.js` 在开发者工具检测命中后执行 location.replace；已在该语句取得实际断点命中及同次调用栈，跳转前只读值为 destination=about:blank、detected=true、probeCount=1、domPreviewHit=false。另外读取确认 Eruda／VConsole 及对应面板均不存在。函数返回断点和控制台事件未取得可用的子探针结果，不能认定究竟是正则对象显示探测还是控制台耗时探测，更不能据此修改官网逻辑。

普通页面读取曾保持正常、Network.enable 后白屏，但另一次新页在首次完整 CDP 命令之前已经白屏，故撤回“Network.enable 是唯一触发点”的假设。未启动生产 Worker、未发送独立职位 HTTP 时也能复现，尚无证据将根因归于 JobHunter 生产连接器。代码检查确认猎聘生产分支不启用 Runtime／Console／Debugger，仅观察 Network 和逐目标读取 Cookie；已有 CDP 测试精确约束命令序列。本轮未改生产逻辑、未读取凭据、未入库，诊断断点清理并关闭本轮临时标签；内置浏览器与官网检测的兼容问题未标修复，T022 仍待在线验收。不通过伪造环境、屏蔽日志或禁用官网检测恢复采集。

2026-09-20 内置浏览器完整 CDP 开启及重启后复核：已看到桌面学生首页登录和推荐列表，Network.enable／readEvents 最初成功，随后同一标签页真实跳转 about:blank；用户也确认可见页面为空白。一次限时导航观察捕获 Page.frameScheduledNavigation、reason=scriptInitiated、url=about:blank，因此不能继续笼统归因于工具地址读取错误，但尚未定位触发脚本或判定规则；没有禁用检测、修改指纹或绕过 CDP 安全检查。

本轮准备内置浏览器 tab 级授权 CDP 到隔离生产 Worker 的内存传输桥，浏览器虚拟 attach 映射不算真实 Chrome WebSocket 握手验收。工具宿主启动 Worker 首次发生 DATABASE_OPEN_FAILED，改用项目 Node 24 后隔离 SQLite 可正常初始化；之后两次连接均因当前页面已不是允许的猎聘首页而失败。两次 requests=0、jobs=0、sync_runs=0、missing_count 非零数=0、外键错误=0，证据库分别为 jobhunter-liepin-unified-live-mZRAzG／jobhunter-liepin-unified-live-jQ5eyx；没有读取 Cookie 或写业务库。44 项猎聘／CDP／生产 CLI-Worker-SQLite 定向回归重新通过，Worker／CLI 类型检查通过，未运行全量门禁。T022 的真实 HTTP 连续批次和整批详情仍未通过；旧记录中的“未暴露 CDP 能力”不再代表当前阻塞原因，后续浏览器路线须由用户确认。

2026-09-20 猎聘完整接入实施计划：完成固定域 CDP 提供方、一次观察初始化、LOGIN→UP 状态迁移、CLI／Web／Worker 装配；补协议／会话隔离／生产队列及浏览器交互定向回归，再用同一授权连接进行最多三批真实 HTTP、整批详情与隔离库验收。重复批次、未知结构、登录墙等异常先停请求并本地分析，不全量抓取、不恢复长期观察；未通过的能力不标成功。

2026-09-20 生产装配代码与本地验收完成，T022 在线部分仍未完成：CLI 注册 liepin、生产 Worker 使用 LiepinCdpSessionProvider、Web 独立 API 与共享来源面板，职位页筛选返回对应猎聘入口。44 项连接器／CDP／生产 CLI-Worker-SQLite 查询定向测试及 4 项猎聘／前程无忧 Web 测试通过，严格 UI 静态审计零问题。初始化一次观察，随后逐目标读取实时 Cookie，HTTP 间隔至少五秒；测试验证 LOGIN→UP、固定查询、末批拒绝、跨批重复冻结、公司身份缺失计数和凭据不落库。官网真实被动样本确认 UP／PC_STU_HP_MIX／existFallbackResult=false，返回主列表 0、补充推荐 40、hasNextPage=true；尚未完成独立 HTTP 连续批次及整批详情验收，不能据此标完整 supported。

用户随后改用 Codex 内置浏览器，已确认 mc.liepin.com 登录和推荐列表正常。当前暴露的内置浏览器工具只有页面交互、资产与 WebMCP 能力，无 Cookie／CDP 登录上下文桥接；不能将移动站页面正常等同于桌面 HTTP 连接可用。后续页面操作改用内置浏览器；原 Chrome 授权研究进程保持空闲，不自动关闭或重连，不自动抓取。需要可用的授权会话桥接后才能继续真实 HTTP 生产验收；长期联合观察保持暂停。

2026-09-20 猎聘最小闭环通过：参考本地 BossHunter 的只读采集、稳定 ID、城市编码与登录墙处理；其实现依赖浏览器 DOM，未直接复制实现代码。当前已登录学生首页提供 `home-recommend-job-new` JSON 推荐接口；`PC_STU_HP_NEW`／`PC_STU_HP_MIX` 排序分别观察，搜索和社招协议未验收。独立 HTTP 首次精简头返回 HTTP 200／flag=0／code=-1400／msg「出错了（400）！」；同时补回已观察的 X-Fscp-Trace-Id 和 X-Fscp-Bi-Stat 后成功，尚未分别证明两项都必需。不发送 UA／客户端提示等浏览器特征头。

真实详情 GET 返回 HTTP 200，HTML 内 `JobPosting` JSON-LD 的完整正文与页面职位介绍一致；校验内部职位 ID、公开 URL、标题、公司 ID 和名称，不执行脚本或调用渲染 DOM。公开 `/job/` 与 `/a/` 身份空间分开；本轮真实详情仅验证 `/job/`，不据此宣称猎头 `/a/` 全面兼容。无公司 ID 的匿名推荐按已观察结构排除并计数，未知类型仍失败。列表主推荐与 addData 补充推荐一并处理，不能因主数组为空误判末页。

过程中出现两次「有成功列表但无可选候选」的研究验收中断，留存隔离库均为零职位；后续只读检查确认上游真实返回两个空数组且 hasNextPage=false。可见推荐流存在服务端状态，不能把原样重复请求当作固定首页或可靠页码。另一次当前构建验收返回 parse_changed，正常浏览器同协议样本确认 40 条中有 6 条合法匿名公司缺省 compId，补充该已知形态的计数排除后重新验证；未删除未知结构校验。长驻进程旧模块缓存造成的初始化异常未触发 HTTP，最终使用新 Node Worker 加载当前构建并复用原授权 Socket。

最终同次真实 Worker 验收：列表 HTTP 200，原始 40 条、有效 34 条、匿名公司排除 6 条；研究装配显式只选择其中一条，详情 HTTP 200，经 PlatformBrowsingService、真实任务租约和 SqlitePlatformRepository 自动保存 1 条完整职位。WebJobQueryService（职位页使用的查询）在 platform／liepin 范围返回 1，默认官网返回 0。独立 HTTP 共两次，起始间隔 5002ms；sync_runs=0、missing_count 非零数=0、外键错误=0。证据库为 `/var/folders/mf/l52yxkf12jx0ycrpdm83k7b40000gn/T/jobhunter-liepin-minimal-sqwq0N`，未写用户业务库。隔离 Worker 已退出，原 CDP 连接保留，凭据／请求体未写入业务库或文件。

当前新增 LiepinRecommendationHttpSession 和 14 项定向回归，具备模板白名单、JSON-LD 身份核验、串行五秒节流、有界读取、取消／失败冻结及正式入库查询隔离。仅实现一个已观察首页批次，不自动重复首页或伪造分页；未注册生产 CDP／CLI／Web 入口，没有把 min-smoke 标成 supported。T022 与联合稳定性观察仍待完成。

2026-09-20 用户允许限量复现以取得错误文本，并要求后续不能只报告错误码。研究生产包装器已增加响应副本有界读取（两 MiB／原请求截止），只输出脱敏普通说明及固定原因字段；正文不可读时明确记录不可读，不以空错误说明冒充“接口没有原因”。不输出检查参数或 Cookie，不为了失败扩大请求预算。

本次新 Worker 加载当前生产 BossCdpSessionProvider／BossHttpSession，复用原 Socket、当前普通认证与适用 Cookie、生产最小头，不点击或刷新浏览器；连续第一页／第二页／第三页均 HTTP 200／code 0、各 15 条有效职位、hasMore=true，无跳过，共 45 个唯一 ID。独立 HTTP 三次，最小起始间隔 5009ms，Cookie 读取四次（初始化一次、逐请求三次），认证字段比较未变化；三次成功响应均带三项检查 Cookie。没有 37、没有失败文本可取；预算结束即停止，没有为了制造错误继续请求。

这是当前生产连接器三页分页的同轮成功验收（非 CLI／Web／入库／详情或长期稳定验收）。此前失败记录仍有效，不能据本轮成功声称根因已修复，也不能编造历史 message。原连接保留，额外 session 清理，入口恢复空闲。后续失败报告应同时提供实际 HTTP 状态、业务码、脱敏 message、附加原因字段及失败阶段；未取得的内容明确标记缺失。

2026-09-20 直接第三页验证成功：沿用原连接，仅从所选浏览器页已有资源时间线读取列表模板及当前普通认证／Cookie，不点击、刷新或触发浏览器新请求；独立 HTTP 首个且唯一请求设为 page=3、pageSize=15，使用生产六类普通头 Accept／Cookie／Referer／token／XHR／zp_token。实际 HTTP 200／code 0／message=Success，15 个唯一稳定 ID、hasMore=true，耗时 573ms；没有先独立访问第一页或第二页，也没有重试或入库。发送前至少五秒等待并复核。本次使用当前浏览器快照及最小头，不能与此前捕获旧 Cookie、保留其他浏览器头的连续分页实验视为仅页码变化的对照；证明第三页并非始终不可访问，不证明前置请求次数或上下文变化的唯一因果。

错误详情核查：本次无错误响应，已知历史原文诊断槽位不存在，最近 37 实验只保留了业务码和检查字段存在性，不能补写当时 message。已为研究输出增加普通 message／msg／errorMessage／description 及固定 reason／reasonCode／subCode／ruleId／riskLevel／retryAfter 标量字段的脱敏投影，不输出检查参数、Cookie 或完整响应；不存在字段时保持为空，不虚构服务端细分原因。没有为收集错误再次触发失败。原连接保留，研究入口恢复空闲。

2026-09-20 三项组合及有界重试结果：第一次完整已观察头首页 code=0，随后同时采用生产 Accept／无查询 Referer／删除 traceId，组合第一页、第二页各 15 条且无重叠，第三页 HTTP 200／37，检查字段 seed／name／ts、无检查 Set-Cookie。合计四次 HTTP，发送 Cookie 始终未变，浏览器安全令牌相对实验前快照未变。保留其他已观察头，故非最小头生产验收。实际每次更新发送时间戳；该轮旧诊断 timestampRefreshed 字段遗漏组合模式，虽输出 false，执行分支与 timestampPolicy 已标发送时更新，之后已修正诊断，不据错误标签推断沿用旧时间戳。

该轮末次实际间隔 4999ms，不能宣称全部不少于五秒。离线回归复现一次计时器等待完成后未复核 Clock，仍在 4999ms 发请求；生产会话和研究等待均已改为循环复核，测试由失败转为通过。这个缺陷修复不等于 37 根因已定位。

用户随后明确允许有限次重试。本次另取新正常官网成功基线，不冒充上一轮失败请求续试：完整头首页成功，三项组合第一页、第二页再次成功共 30 个唯一 ID；第三页初次及四次同参数重试全部 HTTP 200／37，每次均有 seed／name／ts、无检查 Set-Cookie，未恢复。失败页总尝试五次，全实验八次 HTTP，最小间隔 5000ms；重试 URL（含时间戳）、Cookie 和头全部保持不变，未处理检查参数。重试前所选页面路由正常，无显式安全检查参数；这不是浏览器同期接口成功的证据，仍记录 not_observed。期间浏览器 bst／wt2／安全令牌未变化。没有刷新、职位入库、生产模式切换或长期观察。

三项组合成功取得两页，不能把三者组合本身认定为必然触发 37；本样本同参数短间隔有限重试未恢复，也不能推断服务端固定第三页阈值或唯一规则。生产冻结策略未变，仅研究显式参数开放共享四次额外重试预算，默认为零。69 项 BOSS／CDP／平台应用定向测试、连接器类型检查及定向 ESLint 通过；研究合成测试覆盖预算耗尽、零重试、安全路由停止、失败后恢复继续、同请求不变及脱敏清理，全部通过。研究入口恢复空闲，原授权连接保留。

2026-09-20 三项组合及分页复核计划：同轮官网成功列表后，先以完整已观察头请求首页，再同时使用生产 Accept、无查询 Referer、删除 traceId，连续取第一／二／三页；至多四次独立 HTTP，至少五秒间隔。Cookie 与其他头固定，每次在发送前更新时间戳，明确不是逐字节同 URL 对照；组合首页重新开始去重，完整头基线不计入三页结果。任何失败停止，不回填头重试或切换传输；只读继续分析并记录检查字段、失败阶段与上下文变化。此研究仍保留其他已观察头，不能冒充最小头生产验收。

2026-09-20 Accept 独立复核通过：复用原授权连接，正常切换一次推荐条件取得官网列表 HTTP 200／code 0。固定同一第一页 URL（含时间戳）、捕获 Cookie 和其余端到端头，原值 application/json, text/plain, _/_ →生产值 application/json→恢复原值，三次均 HTTP 200／code 0、15 条、hasMore=true，三次返回同一组 15 个稳定职位 ID；两次间隔均 5001ms，首条距官网基线约 10.1 秒。发送前断言头部差异依次为 []／[accept]／[]，URL 和 Cookie 均未改变。

HTTP 前浏览器安全令牌已不同于捕获请求，三次期间浏览器 bst／wt2／安全令牌相对于初始快照未变化；三次成功响应均写入三项非空检查 Cookie，没有回灌或执行安全流程。本轮没有 37 或传输异常。该结果证明本样本可使用生产 Accept，不解释上一轮缺失诊断的异常原因；此前未知记录保留、不回填为网络错误。结合 traceId／Referer 各自的成功单变量样本，尚不能证明同时精简后的头部组合或持续分页稳定，更不能排除服务端隐藏状态。

本轮独立 HTTP 共三次，不翻页、刷新、追加重试或入库，生产请求头未改。捕获内存释放、原连接保留，研究入口恢复空闲；脚本语法、文档格式／一致性和 diff 检查通过。

2026-09-20 Accept／Referer 对照结果：本轮官网成功基线的 Accept 为 application/json, text/plain, _/_，生产值 application/json；Referer 同为 /web/geek/jobs，官网值含查询参数而生产固定值无查询。初始原样独立 HTTP code=0、15 条；第二次发送前断言仅 Accept 改变、URL／Cookie 不变，但实验进入旧脚本的总异常捕获，未保留 HTTP 状态、业务码或失败阶段，因此不能判定 37、内容协商拒绝或网络错误，也不能推断 Accept 必需。该组停止，无恢复控制或原请求重试。

补齐研究记录器的 prepare／wait／fetch／body／parse／validate 阶段、已取得的 HTTP 状态及业务码、耗时、白名单异常名称与网络错误码，不输出原始错误及 cause。独立 Referer 组另取正常浏览器列表 code=0 基线，固定同一请求 URL／Cookie／其他头，原始 Referer（含 jobType 查询键）→生产无查询 Referer→原始控制，三次均 HTTP 200／code 0、15 条、hasMore=true；两次间隔均 5001ms，累计 33 个唯一职位，属于同页重复对照而非分页。发送前分别验证改变头为 []／[referer]／[]。安全令牌在 HTTP 前已与捕获请求不同，浏览器在三次 HTTP 期间相对于初始快照未变化。

本轮合计五次独立 HTTP，Referer 对照通过仅说明本样本无需查询参数；Accept 未形成有效结论，未因未知异常改生产头。没有 37 的可确认新样本，没有追加请求、刷新、检查脚本、职位入库或长期观察；已清理捕获内存并保留原授权连接。脚本语法、文档格式／一致性及 diff 检查通过。此次补充诊断未重跑 Accept，不能补写未知历史失败的原因。

2026-09-20 普通头对照计划：先只读比较实际捕获的 Accept／Referer 与生产值 application/json／固定 jobs 页地址；相同值不测试。不同值分别以原始→单头生产值→原始控制验证，相邻组共享已成功的控制，最多五次独立 HTTP，间隔至少五秒，失败立即停止。各次固定完整 URL、Cookie 及其他头并在发送前断言仅目标头改变；Referer 只输出路径及查询键、不输出查询值。该研究不改变生产配置，不视为持续分页验收。

2026-09-20 列表 traceId 同条件对照完成：原授权连接仍在线，只读页面已恢复职位条件控件；正常点击一次条件后取得官网列表 HTTP 200／code 0，完整 Cookie 及端到端头已捕获。随后固定同一列表第一页 URL（包括 `_`）、Cookie 和其他头，依次原始 A、仅删除 traceId 的 B、恢复 traceId 的 A 控制，三次均 HTTP 200／code 0、15 条有效身份、hasMore=true；两次间隔均 5001ms，第一条距浏览器请求约 19.5 秒。本轮没有翻页，不能把三次第一页响应写成三页验收。同一查询仍返回变化的推荐集合，累计 32 个唯一 ID；顺序实验未固定服务端内部状态。

时序证据：HTTP 前的浏览器快照已与捕获请求的安全令牌不同，bst／wt2 相同；三次 HTTP 沿用完全相同的捕获 Cookie，结束时浏览器三项值相对于 HTTP 前快照均未变化。由此确认本样本的令牌差异已存在于独立 HTTP 开始前，且差异未阻止三次成功；不能把“令牌与浏览器当前值不同”单独判定为 37 的充分原因，也不能反推安全上下文不影响其他请求。三次成功响应均带三项非空检查 Cookie，但不回灌、不执行检查脚本。

本轮结论限于 traceId 在此列表样本非必需，结合此前详情样本不支持盲目将其加入生产修复；尚未判断其他头的必要性，持续分页 37 未解决。独立 HTTP 共三次，不追加重试／请求，不操作验证控件、刷新或写职位库。敏感上下文释放、原授权连接保留；研究脚本语法和文档检查通过。下一项宜单独核对普通 Accept／Referer 值差异，不把客户端特征头批量补入生产，也不声称取得服务端命中规则。

2026-09-20 请求头对照未成立：原授权连接仍在线，正常点击一次 Python 推荐条件后，首次观察器未能取得响应正文，故没有启动 HTTP 对照。首次观察器日志与当前脚本不一致，改为带版本后缀加载当前捕获模块；随后只读页面发现地址进入 `_security_check` 流程且无职位列表控件，未再点击或刷新。立即停止额外监听、清空捕获内存、保留原连接。本轮独立 HTTP 为 0，没有 A/B/A 结果；不能判断 traceId 必要性，也不能把浏览器同期状态记为正常。安全流程的出现只证实当前浏览器基线不满足条件，尚不证明由哪次请求或哪种操作触发。待官网正常浏览恢复后方可建立新的成功基线，不自动反复采样或操作验证控件。

2026-09-20 请求头单变量对照计划：先验证列表 traceId，固定同一捕获 URL（含页码和时间戳）、Cookie 及其他已观察头，最多按 A（原始头）→B（仅删 traceId）→A（恢复控制）发送三次，每次至少间隔五秒。固定页允许相同职位重复，不按分页去重误报；任一步非成功即停止，绝不在 37 后恢复头重试。浏览器上下文前后变化只读记录。该顺序实验仍不能固定服务端内部状态；首请求失败则未形成对照，不判断该头是否必要。不更改生产默认头。

2026-09-20 持续分页复核结果：原授权 CDP 连接在线；第一次正常条件切换时被动捕获窗口已超时，未取得响应，未据此发送 HTTP。重新开启监听后正常切回推荐取得列表 HTTP 200／code 0。保留已观察端到端头，每次独立 HTTP 只读当前浏览器适用 Cookie 和普通 token、更新时间戳，前三页均 code 0、各 15 条，45 个唯一 ID，无重叠；最小起始间隔 5005ms。第一条 HTTP 距浏览器基线约 5.9 秒。三次请求间整体 Cookie 字符串变化，但 bst／wt2／安全令牌相对于 HTTP 前快照均未变化；不能将其他 Cookie 变化当成安全上下文已续期。无检查参数回灌、浏览器代发或刷新。

随后用新 Worker 加载当前构建、原 Socket 桥接生产 BossCdpSessionProvider／BossHttpSession（非 CLI／Web／数据库验收），只验证最多三页、不追加详情：最小普通请求头路径首页 14 条有效候选、1 条匿名公司记录按已有规则排除；第二页 HTTP 200／37，立即冻结，合计两次 HTTP、最小间隔 5006ms、Cookie 读取三次。两次请求的认证字段无变化、时间戳年龄分别 3ms／1ms；37 响应无检查 Set-Cookie。本轮该生产错误未保留 zpData 字段，不能借用上一轮 seed／name／ts 作为此次响应证据。没有重复请求、自动换传输、职位入库或长期观察。

已复现并修复一个独立协议缺陷：列表／详情原先在等待和上下文读取前生成 `_`，离线模拟读取耗时后测试失败；改为实际发送边界生成后通过。官网缓存请求模块亦确认逐请求使用 Date.now；但修正后的生产第二页仍 37，因此不是充分修复。完整头三页成功在先、生产最小头实验在后，还改变了基线年龄及累积请求历史；不能归因为缺某个浏览器头，更不能宣称第三页固定阈值或纯 HTTP 持续稳定已解决。

继续补齐生产 37 的脱敏现场消息：保留业务码及冻结，附传输模式、端点、实际请求序号、此前 code=0 响应数和固定检查字段存在性，浏览器同期状态标 unknown。合成回归覆盖第二页 37 的计数、敏感值不泄露及禁止重试；新增诊断未再次触发真实请求。68 项 BOSS／浏览器传输／CDP／平台应用定向测试通过。原授权连接保留，研究入口恢复空闲；下一步需要可区分变量的成功基线或服务方规则诊断，不能靠重复试请求推断隐藏规则。

2026-09-20 持续分页 37 复核计划：对照已缓存官网普通请求模块，区分固定原请求重放与逐请求更新时间戳、读取现有浏览器认证上下文的行为。限量三页研究必须记录请求前上下文是否变化、成功次数、失败页和检查字段；出现 37 即停止发送，但继续只读分析。不得将完整参数两页成功解释为持续会话通过，不实现安全令牌生成、检查脚本执行或失败后自动切换传输。原授权连接保留。

2026-09-20 完整参数列表翻页对照：用户正常切换条件后取得官网列表 HTTP 200／code 0，本次 Request ExtraInfo 同时捕获 Cookie 及完整端到端头。独立 HTTP 原样保留该请求 URL／Cookie／普通头／已观察客户端头，排除 HTTP/2 伪头、Host、压缩及连接控制字段；首页 code 0、15 条，随后只修改 page=2，code 0、15 条，两页共 30 个唯一 ID、无重叠、hasMore=true。再只修改 page=3，HTTP 200／code 37，zpData 有 seed／name／ts，无检查 Set-Cookie；停止，无重试或浏览器回退。首页距浏览器基线约 37.7 秒，后续两次间隔均 5001ms。原请求 Cookie 在三次 HTTP 中未变化，认证及完整请求参数不是通过猜测构造。

结束后的只读对照显示浏览器 **zp_stoken** 与捕获请求 Cookie 中的值不同，bst／wt2 相同；该轮没有 HTTP 前的浏览器 Cookie 快照，无法确定变化发生时刻，不把它写成“HTTP 过程中浏览器自动续期”或 37 的唯一根因。研究诊断随后增加 before_first_http 快照及前后变化名称（此修正尚未在线重跑），并把 sentCookieChangedBetweenHttp 与浏览器变化明确分开。所有原值仍只留内存，清理后保留授权连接。

本轮结论：已有同轮完整参数下的独立 HTTP 详情及两页列表成功证据；第三页仍失败，所以没有完整三页或整批稳定通过。traceId 在详情样本中可删除，其余字段必要性及会话续期仍待逐项验证；不把完整参数成功解释成所有头都必需，也不把页码与失败时序同时变化的结果解释为第三页固定受限。未写生产职位、未修改生产默认传输、未启动长期观察。

2026-09-20 同轮完整参数 HTTP 对照：首次官网请求正文读取失败（cdp_body 阶段），不作为成功基线；为研究捕获增加有界正文缓冲、重复完成事件保护、主页面导航计数及固定 CDP 错误分类。随后一次正常职位点击取得浏览器详情 HTTP 200／code 0。沿用原 URL（securityId／lid／时间戳不改）和已观察请求头，因 Request ExtraInfo 未捕获 Cookie，显式补入当前选定同站点的适用 Cookie，来源标为 current_scoped_snapshot。

独立 HTTP 第一次保留已观察的 Accept、Referer、token、zp_token、XHR、traceId、User-Agent 与三项 sec-ch-ua 头，约 37.7 秒后返回 HTTP 200／code 0、稳定职位身份相同且正文有效。第二次只删除 traceId，间隔 5001ms，仍 HTTP 200／code 0、身份正文有效；期间不再次读取 Cookie或操作浏览器。两次成功响应均写入三项非空检查 Cookie；完成后只读比较浏览器 bst／wt2／安全令牌与本轮初始 Cookie 快照，均未变化。研究日志早期 comparison 标签仍写 captured_request，但实际比较源为 current_scoped_snapshot，脚本已修正标签。请求凭据及正文均在结束时释放，原授权 Socket 保留。

本轮证实同轮正常浏览器会话与参数可用于独立 HTTP 详情，traceId 在该样本中不是必需字段；未证明其他头必要，也不证明刷新安全上下文的根因已修复。诊断已区分端点、首请求、此前成功次数、code、检查参数存在性、Cookie 写入属性、基线年龄与未知的浏览器同期状态，不给 37 编造子类型。新的列表捕获尚待正常筛选操作，不能将上次旧模块的三页成功归入本轮对照。生产默认 HTTP、停止策略和浏览器模式均未改变。

2026-09-20 HTTP 优先的限量分页验证：同一已授权浏览器仅初始化读取一次登录上下文，没有点击、刷新或浏览器代发。研究进程连续独立 HTTP 第 1／2／3 页各取得 15 条有效候选，累计 45 个唯一稳定 ID，三页 hasMore=true；再抽查首页首条与第三页末条详情，两条身份及非空正文均通过。共 5 次 HTTP，全部成功，最小起始间隔 5000ms；不是推荐流末页测试，没有职位入库。

核查回调计数发现研究进程缓存了旧 BossHttpSession：该次实际没有逐请求 readContext，也没有发送新增 token／zp_token 头，不能写成当前生产代码验收通过。已修正研究门禁：旧模块预检不通过即拒绝发请求，当前实现必须使用新 Worker 加载公开包，原授权 Socket 仍经传输桥复用。

随后新 Worker 以当前生产 BossCdpSessionProvider、显式 http 模式复核：初始化读取 Cookie 一次、首请求前再读取一次，发送 Cookie／Accept／Referer／token／X-Requested-With／zp_token；第一个列表 HTTP 200、业务码 37，无 Set-Cookie 检查字段。请求 1 次，最小间隔 5001ms，立即冻结，未继续翻页或详情，未改生产数据。两轮顺序发生，且认证头与读取方式同时变化，不构成单变量因果实验；不能断言新增认证头导致 37，也不能以旧实现成功覆盖当前实现失败。

37 后继续只读排查：缓存官网模块 97994 → 86344 → 49657 确认普通认证头取值路径；所选页 document.cookie 中的 bst 与生产 Cookie 匹配器用于列表／详情的 bst 解码结果一致，各端点只有一个适用 bst，页面 token 存在且满足有界 ASCII 条件。没有发现可据证修复的普通认证字段偏差；服务端命中规则及安全上下文续期仍未解决。此后无新增 HTTP／浏览器动作，没有尝试执行检查脚本、回灌检查参数或换传输重试。原授权连接保留。当前结论是 HTTP 分页协议可用的短时证据成立，但当前生产纯 HTTP／完整批次／长期稳定验收仍不通过。

- [ ] **PLT-T020**：按 ADR-0031 实现显式 BOSS browser 传输、CLI 选择及有界响应观察；覆盖 PLT-004、PLT-005、PLT-006、PLT-019。真实整批通过后开放 Web 并启动 12 小时观察，未通过不晋级。

2026-09-20 BOSS browser 首次真实整批验收通过：用户确认可开始后复用原授权 Socket，CLI `connect --acquisition-mode browser` → Web 应用发布 `next` → 生产 Worker／生产浏览器传输 → 隔离 SQLite → 统一查询。正常切换既有 Python 求职条件产生新列表，无刷新／重开页面；15 条有效候选全部完成身份及正文校验并逐条保存，跳过 0 条。观察链路正常结束，无 37 或其他任务错误；本轮独立 Node HTTP 为 0，不能将结果描述成 HTTP 续期修复。生产 CDP 命令通过原研究连接桥接，不是另建原生 Socket 的授权验收，也不是 Web 页面模式选择验收。

隔离库 `jobhunter-boss-unified-live-Wr3Xaw`：统一平台查询 15、默认官网查询 0、Web 应用批次摘要 savedCount=15；关闭隔离 Worker 后只读复核，15 个唯一职位、15 条非空正文、2 个任务成功、sync_runs=0、missing 非零记录=0、外键错误=0。生产库未改动。原授权连接及页面保留，临时观察 session 已 detach，研究入口恢复空闲。主动点击由实现强制至少五秒间隔；本次 HTTP 计时器没有采集浏览器请求间隔，输出 minGapMs=null，不将其当成浏览器频率的实测值。

该结果仅证明一批浏览器辅助生产链路通过，不能证明纯 HTTP 稳定、37 的唯一触发规则或十二小时稳定性。Web 新模式入口、后续页及长期观察仍待完成；PLT-T020 保持未勾选，不晋级 supported，未启动定时观察。

2026-09-20 PLT-T020 实现进展：已增加 `connect --acquisition-mode browser`，原 HTTP 默认不变。新模式不导出 Cookie、不发独立 HTTP，通过所选页正常请求的实际 JSON 复用 BOSS 协议校验和既有批次入库；同轮官网自动详情有界复用，其他详情只点击当前批次职位链接。每次主动操作至少五秒间隔，查询变化、跳页、过期正文、身份不符、37、限流、导航及取消均冻结；不刷新或自动换传输。新增取消分类回归，失败后保留原授权 Socket。

本轮 66 项 BOSS／浏览器传输／CDP／应用定向测试通过，CLI／Worker 构建与类型检查、定向 ESLint 通过。当前 Chrome 前台被其他操作切换，尚未启动此次真实整批，以避免误操作其他页面；已有授权连接仍在线。以上仅为实现和离线结果，不证明浏览器模式真实整批或长期稳定，不勾选 PLT-T020，不开放 Web 模式选择、不恢复 12 小时观察。37 仍仅能确认为 SECURITY_CHECK，具体服务端规则及唯一根因未公开。

2026-09-20 同轮浏览器／HTTP 安全上下文对照：用户授权由助手执行正常点击。使用 Chrome 原生窗口操作选择现有 BOSS 页，没有重开、导航或刷新；监听前读取适用 Cookie 基线，点击职位链接后捕获详情 HTTP 200／code 0。浏览器成功响应写入 **zp_sseed**／**zp_sname**／**zp_sts** 三项非空 Cookie；随后读取到 **zp_stoken** 相比点击前发生变化，而 bst／wt2 未变。只使用普通认证头和浏览器当前适用 Cookie，对同一已观察详情做一次独立 HTTP，约 32 秒间隔，HTTP 200／code 0、职位编号与浏览器响应一致；响应同样写入三项非空检查 Cookie。等待 5 秒后再只读浏览器，bst／wt2／**zp_stoken** 全部未变化，浏览器中也没有这三项中间 Cookie。凭据只在内存比较，不落盘；本轮独立 HTTP 总计一次，无入库或后续失败重试。

本轮已验证“独立 HTTP 收到检查参数，不会自动更新浏览器安全令牌”；浏览器普通请求后的令牌变化与独立 HTTP 后的不变化来自同一轮前后快照，而非拼接历史样本。未比较两份响应检查参数原值是否不同，也未继续请求直至 37，因此不能据此宣布唯一触发规则或失效次数已经确定。页面最终仍显示原先的“强化学习算法”，捕获及 HTTP 对照的详情为“AI游戏资产Python后端（AI Agent）远程办公”；本轮证据限定为浏览器实际接口响应与 Cookie 状态，不声称完成了该职位的可见详情切换验收。原授权连接和页面保留，研究上下文清空。

2026-09-20 继续排查具体触发规则（没有新增独立 HTTP）：读取缓存官网 Cookie 工具模块，确认安全令牌写入参数 3840 的单位为分钟，即客户端 Expires 约 64 小时；它不是服务端认可令牌的期限，不能据此认定令牌持续有效或把短时 37 解释为 Cookie 自然过期。分别进行了 60 秒、180 秒被动窗口，每 5 秒只读适用 Cookie：bst／wt2／**zp_stoken** 在各窗口内均未变化、无一分钟内到期项、顶层导航为 0；两个窗口均未捕获新的目标列表／详情请求。用户点击产生的请求位于窗口之外，随后资源时间线只读核对了这一时序，不能将该点击与窗口 Cookie 样本拼接为“成功请求后令牌不变化”的结论。

历史失败发生在首个列表、第二／三／五条详情等不同位置，不支持固定的本地第 N 次请求阈值，但不能排除跨会话计数、频率、IP、账号或环境因素参与综合判断。客户端只公开 SECURITY_CHECK 分类与检查上下文，现有证据没有具体命中规则 ID／评分／阈值；尚不能认定唯一根因。为下一次获准的限量 smoke 补了研究层相邻请求认证字段变化布尔诊断（无原值），本轮未执行该 smoke、未修改生产代码、未刷新页面。观察已结束并释放额外 session，原授权连接保留。

本轮定向验证：51 项 BOSS／CDP／平台应用测试、连接器类型检查、四个实现／测试文件 ESLint、格式、文档一致性与 diff 检查通过；覆盖上下文更新、旧 token 清除、头部注入、跨域、读取失败、取消、初始化期限之后的同连接复用及 37 冻结。未运行全量门禁；离线通过不替代下述失败的真实整批验收。

2026-09-20 修复后生产装配结果（仍未完成整批修复）：普通认证头、每次 HTTP 前同一 CDP 只读上下文已实现；初始化 120 秒期限与后续请求信号分离，取消／读取失败／跨域均禁止使用旧凭据。使用新构建连接器、真实 CLI connect → Web 应用 next → Worker → 隔离 SQLite，原 CDP 仅作传输桥接；一批列表与第一条详情成功，第二条详情 HTTP 200／code 37，立即冻结，没有追加上游请求。隔离库 `jobhunter-boss-unified-live-VxePpN`：请求 3 次、最小间隔 5000ms、正式职位 1、sync_runs=0、missing 非零记录=0、外键错误=0。生产库未修改，原授权连接保留。

当前结论：普通认证对齐和读取浏览器最新 Cookie 能成功获取数据，但不足以稳定完成整批；不能把这一轮称为 37 已解决。正常浏览器及独立 HTTP 的成功响应均确认写入三个非空安全检查 Cookie，而独立 HTTP 响应不会自动改变浏览器持有的安全上下文；只读回调无法自行完成该 HTTP 响应对应的安全状态更新。该结构差异已证实，但具体服务端触发规则及本次失败的完整因果仍不可见。保留已验证的协议修正，不实现安全脚本执行、检查参数回灌浏览器或失败重试；完整验收及 supported 仍未通过。

2026-09-20 BOSS 最小上下文修复计划：正常浏览器详情 code=0，完整应用头与当前适用 Cookie 的独立 HTTP 同样 code=0、身份正文有效；再移除浏览器特征头与 traceId，仅补 token／zp_token／XHR 标识，生产 BossHttpSession 一批列表及一条详情均成功，候选 10 条。浏览器及 HTTP 成功响应的三项安全检查 Cookie 均为非空写入；浏览器后续 Cookie 快照中三项已清理。以上不证明所有 37 均来自认证头缺失，也不证明整批稳定。实现范围为补普通认证、请求前只读同一浏览器当前上下文，保留 37 冻结；覆盖凭据变化、取消、跨域、120 秒后的连接复用和敏感值不泄露，再做定向生产装配 smoke。

2026-09-20 BOSS HTTP／37 协议诊断：沿用已授权 CDP，只读当前页面缓存的 v6740 官网脚本，没有打开、导航或刷新页面。由列表／详情模块 97994 → 通用请求模块 86344 → Axios 模块 49657 追踪实际调用；安全处理模块 7364 明确把 37 定义为 SECURITY_CHECK，31／32 分别为 IP_BLOCK／UID_BLOCK，35／36 分别为 IP_GRAY／UID_GRAY。因此 37 是服务端要求安全检查，不等同于未登录、IP 封禁或职位解析变化；具体命中的服务端规则未向客户端公开，不能直接归因为请求频率、UA 或 TLS。

官网实际请求包含 X-Requested-With、来自 bst Cookie 的 zp_token、条件性来自页面上下文的 token，以及 traceId；生产 BossHttpSession 当前仅发送 Cookie／Accept／Referer。只读确认 bst 和页面 token 均存在，不输出其值。本轮仅一次独立 HTTP 首页对照：复用生产 URL 构造、Cookie 匹配和响应解析，临时补 zp_token／X-Requested-With，仍为 HTTP 200／code 37，zpData 字段为 seed:string、name:string、ts:number，Set-Cookie 为空；没有详情、重试或入库。这个结果只说明补这两个头不足以恢复当前访问，并非完整应用请求对齐，也不能证明所有认证差异均无影响。研究包装器最初以错误属性 code 读取 PlatformError，停止日志误标 diagnostic_failed；原响应日志已明确记录 37，生产异常实际字段为 category／businessCode，不能将包装器标记理解为网络失败。

官网实际响应处理还维护安全令牌：存在完整安全检查 Cookie 上下文时，由官网浏览器逻辑更新 **zp_stoken** 并清理三个中间 Cookie；本项目独立 HTTP 不处理这一浏览器安全检查流程，Cookie 只在初始化复制。此前成功响应出现 **zp_sseed**／**zp_sname**／**zp_sts**，但仅保留名称，不能证明其为写入而非删除，故“缺少安全上下文更新导致后续 37”仍是假设，不实施猜测性的 Cookie jar 或脚本执行。下一步证据应来自用户正常浏览的成功请求／响应：在内存比较完整普通认证字段与 Cookie 属性，再判断仅借用浏览器更新后的最小上下文是否足够；不为了取得成功样本重复刷新或请求，不生成、伪造安全令牌，不规避检查。本轮未改生产代码、未晋级 supported、未恢复稳定性观察，原授权连接继续保留。

2026-09-20 BOSS 深入排查与环境对照：仅用已有 CDP 读取浏览器缓存脚本，成功取得 vendor-1 对应导航位置，附近包含原生对象／HTMLDocument／HTMLBodyElement／HTMLHtmlElement 检查、method_modify 事件，以及 window.location 写入、内容隐藏等逻辑。确认不是采集代码的显式刷新循环；具体触发检查项仍未确定，未解码或修改检测逻辑。BossHunter 的详情主要通过正常浏览器导航及 DOM 读取，不能用其机制证明本项目 Cookie → 独立 HTTP 的稳定性。

不使用浏览器检查工具的单批复核：列表及 4 条详情成功，第 5 条详情返回 access_blocked (37)，共 6 次独立请求、最小间隔 5000ms；隔离库 `jobhunter-boss-unified-live-YtxrkW` 保存 4 条完整正文，平台查询 4、默认官网 0、sync_runs=0、missing 非零记录=0、外键错误=0。成功响应观察到三项 Set-Cookie 名称，但本次仅采集名称，不能认定是刷新、删除或 37 根因，未据此猜测性实现 Cookie 更新。额外 15 秒内每 5 秒读取一次适用浏览器 Cookie，13 项均未变化、无一分钟内即将过期项，只输出计数和变化名称，不持久化值。

用户明确关闭 BOSS 开发者工具后，同一目标被动观察 30 秒 Document 请求及顶层导航均为 0；保留原授权连接、不打开／刷新／检查页面，当前生产装配再取一批，首个 HTTP 200／业务码 37，即停止，无详情请求。隔离库 `jobhunter-boss-unified-live-hKd0DH` jobs=0、sync_runs=0、missing 非零记录=0、外键错误=0，最小等待 5001ms。页面已稳定仍不能证明纯 HTTP 访问恢复，不能将二者视为同一已修复问题。23 项 BOSS／平台应用定向离线回归通过；不修改解析校验、指纹、认证、不自动重试或回退浏览器，整批验收及 supported 仍未通过。生产数据未变，原 CDP 保留，研究入口恢复空闲。

刷新诊断追加证据：停止只读页面操作后的被动 30 秒窗口仍有 12 次顶层导航，均为 script 发起；调用栈指向 BOSS 站点脚本 `vendor-1.a73b9143.js`（零基行 4、列 8440）及 `main.js`（零基行 0、列 660488）。其后被动 5 秒窗口出现 1 次相同导航，再后 5 秒窗口为 0。用户 Network 瀑布截图也显示重复资源加载组。已确认网站脚本导航而非单纯页面重绘，但尚未读取成功对应脚本正文，因此具体触发条件（检查工具交互、会话校验或其他逻辑）未定，不直接归因为反调试。尝试读取浏览器缓存脚本未成功，未为取脚本而重开／刷新页面；本轮无独立职位请求，研究入口复原空闲。

2026-09-20 浏览器持续刷新诊断：用户澄清是同一页面反复刷新，而非重新打开。复用原授权 CDP，被动观察同一 BOSS 顶层 frame 的 Document 请求、frameNavigated 和 scheduled/requested navigation；不发职位 HTTP、不调用刷新。首个静置 30 秒窗口文档请求／顶层导航均 0；第二个 30 秒窗口执行只读浏览器状态、DOM 检查和截图，出现 6 次 Document 请求及 6 次顶层导航，6 次请求 initiator 均为 script、导航原因为 scriptInitiated，截图可见加载骨架。由此确认存在真实脚本导航，并与浏览器检查组合操作相关，不能再以“无显式 reload”否认。该组合尚未逐项拆分，也未捕获当次调用栈，不能断言网站反调试或具体扩展脚本是根因；独立职位 HTTP 并非此次现象的必要条件。停止该浏览器检查路径，后续诊断仅保留被动 CDP 观察，不重复触发刷新。

2026-09-20 BOSS 再次真实复核：同一原授权连接、独立首页及两条详情成功，第三条详情 HTTP 200／业务码 37，按 access_blocked 冻结，共 4 次独立 HTTP、实测最小间隔 5001ms。隔离库 `jobhunter-boss-unified-live-4YbhvQ` 保存 2 条，sync_runs=0、missing 非零记录=0、外键错误=0。用户反馈浏览器反复刷新后停止所有页面操作与后续采集，研究入口恢复空闲，CDP 保留。核对当前研究 Worker／桥接／父进程没有 reload、Page.navigate 或定时导航命令；但标签失效后确实新建并打开过推荐页，此操作造成重新加载，不能笼统宣称“没有刷新”就等于没有页面加载。至少 5 秒约束针对独立职位 HTTP，不覆盖浏览器自身加载的资源请求。页面反复加载的全部触发因素尚未确定，后续禁止用新建／重开页面代替标签失效诊断，不自动刷新或重开用户页面。整批仍未通过，不恢复后台观察。

2026-09-20 BOSS 整批续验仍未通过：原授权连接在线，无新授权；原首页标签不可用后建立唯一推荐页，等待其自行加载到正常列表，没有 reload 或循环导航。沿用同一 CDP 传输桥接当前生产装配，一次列表 HTTP 200 且解析成功，首条详情在约 7782ms 后 fetch TypeError、尚无 HTTP 响应，调用信号未取消；底层代码不在诊断白名单内，固定输出 other，无法据此判定 DNS、TLS、连接重置或访问限制。此次不是已观察到的业务码 37，也不是 parse_changed；未知原因不放宽解析或伪造完成。

整轮独立请求 2 次，实测最小请求间隔 5000ms，失败立即冻结，无重试、翻页或后续详情。隔离库 `jobhunter-boss-unified-live-GTP6DE`：jobs=0、sync_runs=0、missing 非零记录=0、外键错误=0；此前保存的隔离证据不受影响，不把多轮部分结果拼为整批通过。原授权连接保留，研究观察关闭，入口恢复空闲；生产库未修改。仍需稳定会话下完成单批全详情，当前不晋级整批成功或 supported。

2026-09-20 BOSS 统一批次真实复核未全通过：用户确认登录后，只读页面可见正常推荐和详情；旧两条 CDP 均已断开，经用户允许新建一条持久连接。原推荐标签随后不可用，新开推荐页呈加载状态但生产连接器取得有效请求上下文。复用授权传输桥接执行当前真实 CLI connect → Web 应用 next → 生产 Worker／BOSS HTTP／隔离 SQLite，不重放响应、不改 Cookie 或请求参数，仅尝试一批。列表和前两条详情均成功，第三条详情 HTTP 200 但业务码 37，任务以 access_blocked (37) 失败并立即冻结；共 4 次独立请求，至少 5 秒间隔，没有重试、翻页、自动刷新或追加详情。

两条已成功详情自动保存并保留完整正文；本地 Web 查询 BOSS total=2，默认官网及智联 total=0；sync_runs=0、missing 非零记录=0、外键错误=0。隔离证据库 `jobhunter-boss-unified-live-CS7irH` 保留，生产数据未修改。此次证明部分入库、失败冻结及查询隔离，不证明整批成功或长期稳定。37 是已识别的上游访问限制，不是 parse_changed；无法仅凭该码确定触发因素，不通过放宽校验或重复请求掩盖。研究 Worker 已结束，原始新授权连接仍在线且观察关闭，研究入口恢复空闲；未改生产代码或运行全量门禁。

智联网络诊断修复计划：先白名单采集受限在线失败原因，不重试失败请求；补齐主站网络异常脱敏分类与取消语义，以合成连接重置、超时、正文读取失败、凭据污染异常和失败冻结做回归；真实整批复核与原失败根因分别记录，不因一次通过宣称稳定。

2026-09-20 继续排查：旧实现把非 PlatformError 统一转换为 network_error，丢失底层代码；原异常未保留，无法追溯上次第二条详情失败的具体原因。本轮复用原授权 CDP，使用同条件 java 搜索与传输诊断白名单限量复核：首页 20 条、20 次实时详情全部 HTTP 200，共 21 次独立请求、最小间隔 5000ms；生产 Worker 自动保存 20 条完整正文，Web 批次 savedCount=20、平台查询 total=20、默认官网 total=0，sync_runs=0、missing 非零记录=0、外键错误=0。隔离库 `jobhunter-zhilian-unified-live-6M9DCD` 保留，原连接在线且观察关闭，生产库未变。该在线运行使用补丁前的生产构建，证明整批链路此次可用，不证明诊断补丁修复了原网络故障。

已补齐智联主站固定网络原因码，覆盖系统及 fetch cause 的有限白名单、未知原因、请求超时和正文流错误；取消不再被误报为网络异常，失败仍冻结且不自动重试。未改变身份校验、签名、请求字段、间隔或超时，没有新增浏览器回退。智联连接器、CDP、平台应用及生产 Worker 共 74 项定向回归通过，Worker／CLI 类型构建、定向 ESLint、格式与文档检查通过，未运行全量门禁。本轮未复现原网络故障，不把它归因为风控、连接重置或超时，长期稳定仍待验证。

2026-09-20 统一批次自动入库真实 smoke：按用户要求先验前程无忧与智联，各限定首页一批，不自动翻页；复用原已授权 CDP WebSocket，以传输桥接装配当前生产 Worker／连接器、真实 HTTP、CLI connect 和 Web 应用 next／查询服务，未替换上游响应。独立请求间隔至少 5 秒，隔离 SQLite 保留证据，生产库未改；本轮未重新执行 Web 页面点击验收或全量门禁。

前程无忧通过：沿用 Java、广州／深圳／成都及综合排序，官网正常回首页后一次独立 HTTP 200，20 条候选自动补齐并保存 20 条完整正文，无须用户点击详情保存或额外详情 HTTP。Web 批次 savedCount=20、平台查询 total=20、默认官网查询 total=0，sync_runs=0、missing 非零记录=0、外键错误=0。隔离库 `jobhunter-51job-unified-live-Qqvs7k`。

智联部分失败：主站全国 java 搜索、默认排序；首次初始化未收齐模板而超时（session_unavailable、独立 HTTP=0），复用原授权连接重新初始化后成功。一次首页列表与第一条详情均 HTTP 200，第二条详情在收到 HTTP 响应前报 network_error，立即停止，不重试、不翻页；共尝试 3 次独立请求，保留已成功入库的 1 条职位。只读复核 Web 智联查询 total=1、默认官网及前程无忧查询均 0，sync_runs=0、missing 非零记录=0、外键错误=0。隔离库 `jobhunter-zhilian-unified-live-V5CDlu`；未保存底层传输异常，不能断言是风控、超时或连接重置。本轮证实部分成功保留与来源隔离，但智联整批成功仍未通过，不改变长期稳定性结论。临时 Worker 已结束，原授权连接保留且观察关闭，研究入口恢复空闲。

2026-09-20 前程无忧官网辅助完整链路验收通过（PLT-T018）：遵照用户决定暂不追查未复现错误，不修改解析规则。复用 PID 28100 的既有授权 WebSocket，通过真实 CDP 命令桥接同一个生产 Worker，会话 generation=2；CLI 连接后，由真实生产 Next 页面／API 发布动作、生产 Worker 消费任务，未 mock HTTP、连接器或返回值。官网正常回首页，再正常翻到第 44 页：Web 两次“读取官网批次”分别获得 20 条／16 条、hasMore=true／false；末页页面明确“已无更多职位”且继续读取禁用。总计两次独立 HTTP，均 200、间隔至少 5 秒，无中间页遍历、自动刷新、重试或额外详情 HTTP。

两批各通过 Web“读取详情并保存”保存一条，来源链接切换为“查看已保存职位”；从首页候选进入统一职位详情页，看到真实标题、正文及官网链接，可见浏览活动已记录。正式库记录两条正文长度 134／1023，首页已存职位在末页读取后仍存在，missing_count 均 0；5 个平台任务全部 succeeded、sync_runs=0、外键错误=0。隔离证据库 `jobhunter-job51-web-live-fjStR9` 保留，生产库未更改。临时 Next／Worker 收尾，额外 CDP session 释放，原授权连接保留且不后台采集。历史 parse_changed 根因仍未知，仅保留诊断；本次通过不宣称彻底修复或长期 supported。后续推进猎聘独立最小闭环；联合观察仍保持暂停，待全部平台接入后再按 12 小时／每 3 小时方案执行。

2026-09-20 parse_changed 诊断与受限复核：上轮生产连接仍在线但会话已冻结、响应已释放且脚本不支持热加载；保留它并经用户再次授权建立可复用诊断连接。正常翻至第二页、首页各执行一次独立 HTTP，均 HTTP 200、application/json、业务 status 字符串 1、总数字段为 number、20 条且正文无 HTML 标签；原生产解析器均成功，内存中仅替换 MIME 的离线对照也成功。此次未复现上轮 parse_changed，不能认定具体数据字段、网站风控或 MIME 是根因。

本轮修正诊断缺口：parse_changed 附带固定阶段码，区分模板、查询变化、媒体类型、响应大小、JSON、信封、职位结构、分页和身份；不记录响应原文或动态值。畸形详情 URL 的 URL 构造异常改为 parse_changed/detail_url，不再误报 network_error。保留原严格字段、正文、分页及冻结策略，未以扩大 schema 掩盖未知问题。

增加 9 项脱敏／冻结回归，连接器与 CDP 定向测试共 49 项通过。第三次独立 HTTP 由更新后的生产 Worker／CLI 执行：通过已有授权 WebSocket 桥接真实 CDP 命令，未替换连接器、HTTP 或上游响应；正常打开第 44 页，HTTP 200、16 条、hasMore=false；选取正文后正式保存，统一查询与 WebPlatformService.saved 投影均通过，正文 1023 字、jobs=1、missing 非零记录=0、外键错误=0。隔离库 `jobhunter-job51-fix-live-ZRhffZ` 保留，生产库未改；这次是末页单批真实保存，不是同一生产会话首末页或长期稳定验收，也未重新执行 Web UI 成功流程。PLT-T018 仍不标全量完成。全轮独立请求共 3 次，间隔至少 5 秒；研究凭据与响应清空、观察关闭，两条授权连接均未主动关闭。原错误根因仍未确认，不声称彻底修复。

列表 parse_changed 修复计划：用一次有界真实响应在内存中确认 MIME／JSON 信封／职位字段／分页的失败阶段，固定脱敏结构后增加回归；只修正已证实的兼容性，不取消正文、身份、分页与失败冻结检查。使用同一内存响应做修复前后对照，真实生产续验单独记账，不把响应回放当作新增在线成功。

2026-09-20 真实生产链路续验未通过：新授权仅建立一次原生 CDP，使用真实 `createProductionWorkerApplication`、CLI 子进程与隔离 SQLite（`jobhunter-job51-production-live-jGO8pr`），没有替换连接器或重放上游响应。`connect` 成功；官网同条件搜索未产生新请求，单次正常刷新后观察到首页请求，独立 Node HTTP 在至少 5 秒间隔后返回 HTTP 200，但 `next` 以 `parse_changed` 失败。没有自动重试、末页请求或详情请求，整轮独立上游请求共 1 次。现有固定错误未指出具体校验分支，未保存响应原文，因此不能认定是官网字段变化、HTML 验证页或具体字段问题，也不能据 HTTP 200 判定采集成功。

失败链路验收通过：隔离库 jobs=0、sync_runs=0、missing 非零记录=0、外键错误=0，BOSS／智联连接状态不变。启动独立生产 Next 服务读取同一隔离库，实际 Web 来源页显示“连接不可用”、读取按钮禁用与停止请求提示；任务页显示 `parse_changed`、尝试 1/1。只读验证后停止临时 Web，保留 Worker 与已授权 CDP（connected=true）；生产业务库未修改。PLT-T018 仍待真实列表／详情／首末页成功验收。下一步需在内存增加脱敏的响应类型和校验阶段诊断，再限量复核，不应放宽校验来掩盖错误。

2026-09-20 前程无忧工程实现完成、在线验收待复核：新增受限 HTTP 会话与持续的所选页请求观察器，注册 Worker／CLI／Web 的 `51job` provider；JSON 正文经选取后进入统一职位库。已通过连接器／CDP 40 项单测、CLI 与生产 Worker 隔离装配 5 项测试、Web API／桌面与窄屏交互 4 项浏览器测试；后两组使用模拟上游，不代表真实生产 HTTP 已通过。定向类型检查、Web 构建及 UI 审计通过。复用现有平台组件与交互契约，没有新增视觉体系。

本轮正常浏览器列表成功，但一次原始模板独立 HTTP 在收到响应前报 ECONNRESET，随后停止所有在线尝试。将当前浏览器已收到的 JSON 仅在内存离线回放给新解析器，得到 20 条及有效正文，只证明真实结构可解析。PLT-T018 保持未完成；自动翻页、生产在线完整链路及长期稳定性不得标记 supported。未修改生产库或启动全量门禁。收尾时原授权连接已收到 close 事件，状态 connected=false，原因未确定；没有主动关闭或自动重连，研究凭据已清空。

使用入口：Web 来源页选择“前程无忧 · 官网辅助”；CLI 使用 `pnpm platform --provider 51job`。连接官网搜索标签后，先在官网搜索或翻页，再执行 `next`／“读取官网批次”；选择候选详情后正式保存。切换查询须重新连接；同一连接最多消费 20 批，无新模板最多等待 90 秒，失败冻结而不后台重试。

前程无忧工程化计划：实现原始请求观察器与受限 HTTP 会话 → 定向边界／分页／详情测试；装配 Worker、CLI、Web → 隔离任务和浏览器状态测试；真实 smoke → 最多三批且不重试风控。当前一轮原始模板 HTTP 返回 ECONNRESET，无 HTTP 状态，停止在线请求并单独记录，不影响离线实现，不将历史成功替代本轮在线验收。

2026-09-20 清理异常修复完成：直接执行临时研究脚本并替换文件读取、输入和 WebSocket，合成失效 session 的 CDP 错误，复现旧 idle 未捕获拒绝后退出。修复后清理失败输出固定脱敏阶段，仍释放内存并响应 status／重复 idle；显式关闭 detach 失败仍释放 socket 和输入。四项离线脚本回归通过。真实原始日志未保留 CDP 错误码，因此只能确认拒绝未捕获这一机制，不能确认当时标签失效的具体原因。

生产连接器没有研究脚本的 idle 命令，初始化失败已有受控错误转换和 finally 清理；新增失效 session 的生产回归，20 项 CDP 测试全部通过，无须修改生产实现。相关类型、ESLint、格式检查通过。本轮未连接 Chrome、未新增授权或上游请求；已退出的旧连接不能恢复，下次真实验证仍需重新授权。研究脚本及其四项回归位于忽略提交的 `var/research/`，生产回归位于 `packages/platform-connectors/test/cdp.test.ts`。

2026-09-20 前程无忧最小研究闭环通过：复用原授权 CDP，正常翻到第二页捕获官网 `search-pc` 请求，浏览器 HTTP 200／业务状态字符串 `1`／20 条。保留已观察 URL 和应用请求头作一次独立 Node HTTP 对照，HTTP 200／状态 `1`／20 条／总数 872；未生成签名或修改风控字段。第一条职位 172838369 的 `jobDescribe` 为 557 字完整职责和任职资格，与官网详情可见正文一致，故消费本次列表正文而不重复详情 HTTP。

同次 HTTP 内存事实经临时会话桥接真实通用 Worker、平台服务和 SQLite，完成 connect → next → detail → disconnect；统一查询读到正式职位，候选 20、jobs=1、sync_runs=0、missing_count=0、外键错误 0，检查的五张持久表未见借用 Cookie／令牌等认证头值。隔离库 `jobhunter-51job-minimal-qxAXyk` 保留；生产库未变。此结果证明协议研究及隔离保存可行，不是生产 51job 连接器／CLI／Web 装配验收，也未验证凭据最小化、签名跨页生成或长期有效性，PLT-T018 继续未完成。观察及临时附加会话已释放；随后 idle 清理原智联会话返回 CDP 错误，未捕获异常使常驻研究进程退出，原授权 WebSocket 未能保留。已给临时研究脚本补充失效标签清理容错，未自动重连；下一轮需要重新授权。此次没有全量采集或全量门禁。

2026-09-20 前程无忧恢复复核：用户反馈正常后，确认新打开的 www 首页已显示职位；从首页打开一条职位，详情短暂显示“验证通过”后正常跳转，截图确认职位正文可见，未操作验证码。原 we 搜索页起初仍为空，单次正常刷新后 Java 列表及筛选控件恢复。更正此前阻塞状态：浏览器首页、搜索与单条详情当前可用，不代表独立 HTTP 或生产接入已验证。此次未发独立 HTTP、未写业务库，原研究 CDP 连接仍存活且观察关闭；PLT-T018 保持未完成。

2026-09-20 用户决定暂时跳过 PLT-T017，不追加智联请求；改为推进前程无忧最小闭环及接入。参考本地 BossHunter 的 `collection/platforms/job51.py`：候选协议为 `we.51job.com/api/job/search-pc`，列表可能包含 jobDescribe；这只是研究线索，必须以当前官网和独立 HTTP 验证为准，不复制其大规模采样／断点扫描或将经验阈值视为平台保证。既有授权研究连接继续保留。

前程无忧本轮受阻：现有官网首页显示已登录及职位推荐，但新开 `we.51job.com/pc/search` 在 Chrome 报 ERR_CONNECTION_CLOSED，一次无凭据 HEAD 连通性检查在 TLS 阶段报 SSL_ERROR_SYSCALL。随后仅从原首页正常搜索一次 Java，进入官网生成的搜索 URL；加载结束仍只有导航／页脚，没有搜索控件和职位列表。只读资源时间线中没有 search-pc 请求；部分初始化请求（含 cupid 的 person-info）约 10 秒后 transferSize=0，不能仅凭资源 status=0 推断 HTTP 状态或风控。未读取这些个人接口响应、Cookie 或简历。尚未取得当前搜索模板、真实职位 JSON 或独立 HTTP 成功证据，PLT-T018 保持未完成，生产 51job 入口不开放。没有刷新、解验证、切代理、直接猜测调用职位 API 或运行 BossHunter 采集器；保留原用户搜索页供人工核验，原授权连接不关闭。需要官网搜索正常后继续最小闭环。

2026-09-20 智联主站工程化：新增受限 ZhilianSearchHttpSession，CDP 根据校园 `/recommend` 或主站 `/jobs` 固定协议；主站同时观察列表 POST 与详情 GET，验证同一认证上下文后停止监听。CLI `--provider zhilian`、生产 Worker、Web 入口共用原有任务与来源身份，不新增库表。主站仅开放已观察的关键词、城市、经验筛选与默认排序；未知字段拒绝，筛选改变须显式重新连接。共享前端只改能力提示和智联“下一批职位”文案，BOSS 行为不变。校园与主站间隔均改为至少 5 秒；主站 JSON 的 description 实际也含 HTML，使用 jobDesc 并限量支持无属性 div，校园原标签边界不变。

独立详情研究 HTTP 200／code=200／apiCode=200，取得职位 CC133807240J40965742615 与公司 CZ133807240。第一次生产装配使用原授权连接的传输桥，真实 CLI 连接与第一页成功；第二批在取得 HTTP 响应前失败 network_error，任务耗时 14821ms，立即停止，未读详情或写职位。未取得底层异常码，不能认定为风控或某个确定网络原因。失败隔离库 `jobhunter-zhilian-social-live-ODgFX7` 保留。

新增真实 5 秒计时回归确认：前一 Worker 任务信号结束不损坏后续会话。随后另一次显式限量单批／详情验收成功：新初始化仍复用原授权 WebSocket，生产 CDP 监听真实官网搜索和详情请求（没有模板／响应重放）；CLI 发布 connect／next，WebPlatformService 发布 detail，真实生产 Worker 完成。两次独立 HTTP 均 200，最短间隔 5000ms，列表 20 条、选中“开发工程师”正式保存并可统一查询，Web 投影 saved 一致。隔离库 `jobhunter-zhilian-social-live-qjPqp3`、职位 `01a0bac4-cf9f-7b3f-bfc3-2d1d6b4976ad`；jobs=1、sync_runs=0、missing_count=0、外键错误=0，五张持久表未见检查的观察令牌。浏览器侧主站搜索可能混有实习，此次保存选中的是 3–5 年经验职位，不推断整个结果皆为社招。

本轮在线合计一次研究详情、第一轮两次列表尝试（第二次未收到响应）、第二轮一批列表与一条详情；另有正常官网搜索自动请求。不再追加请求，不跑全量门禁，不迁移生产库或恢复稳定观察。临时 Worker 均退出、额外页面 session detach、私有研究上下文清空，原 PID 17130／终端 71212 仍 connected=true、observing=false。生产多批的失败未被单批成功覆盖，PLT-T017 保持未完成，不晋级长期稳定或 supported。

定向验收：100 项连接器单元测试、4 项 CLI／生产装配隔离测试、4 项本地浏览器测试通过。浏览器测试覆盖真实本地 API 的 CSRF／provider 隔离及合成状态下的保存、失败恢复、幂等、隐藏页停轮询；不是线上账号全流程 UI 测试。1280／768／390 宽度 axe 无违规，截图检查沿用共享面板与既有样式。相关类型、ESLint、格式、Web 生产构建、依赖边界、严格 UI 静态审计和文档检查通过。真实上游证据与这些离线／本地测试分别记录。

2026-09-20 搜索／多批分页限量复核：校园推荐使用生产 ZhilianCampusHttpSession，首页 20 条、第二页 15 条，第二页 end=1／hasMore=false；35 个唯一职位，无页内或跨页重复、排除 0 条。第二页已是真实末页，不再请求第三页。本轮未经过 Worker／数据库，不能视为新增入库验收。

主站搜索通过官网 UI 设置 Java、成都、3–5 年经验，观察到实际 POST `https://fe-api.zhaopin.com/c/i/search/positions`（platform=13），不是 BossHunter 旧 `/c/i/sou`。借用该次请求模板进行独立 HTTP：首页／第二页各 20 条，总数 374；直接抽查计算所得第 19 页返回 14 条且 isEndPage=1。三页均 HTTP 200、code=200、data.statusCode=200、isVerification=0；54 个唯一职位，无页内或跨页重复，经验字段均为 3–5 年且公司编号非空。未遍历中间页；首页首条身份与可见官网卡片相符，未声称整页 JSON 与浏览器响应逐条一致。

独立 HTTP 共五次（校园两次、主站三次），请求间隔至少 30 秒；另有正常官网导航、搜索和筛选自动请求，不刷新、不重试。主站未携带 Cookie 头，但保留原始 URL／正文的认证及上下文，尚未验证最小字段或长期有效性。官网自动详情 GET `/c/i/jobs/position-detailv3` 仅观察到 HTTP 200，未独立验证。未新增主站生产连接器／CLI／Web 搜索能力、未写业务库或恢复稳定性观察。临时本地消息解码观察已还原，额外页面会话已 detach，研究数据清空；原授权 WebSocket 保持 connected、observing=false。仅更新证据，不晋级 supported。

2026-09-20 智联校园生产闭环完成：新增受限 CDP 请求监听初始化，与 BOSS 共享传输但独立会话；生产 Worker 注册 platform.zhilian，CLI 支持 `pnpm platform --provider zhilian`，Web 复用同一面板、增加独立 provider URL／API。未开放主站搜索、51job 或猎聘。新增 ADR-0028；平台切换不关闭另一个会话，隐藏页不轮询，提交不确定时复用幂等令牌。

真实在线验收使用既有授权 WebSocket 的内存传输桥，未重新建立或关闭 Chrome 连接。生产 ZhilianCdpSessionProvider 监听一次正常热门分类切换并校验模板；桥仅替换 WS 传输所有权和 attach／detach，不替换 HTTP、任务处理或响应。真实 CLI 发布 connect，Web 显示已连接；从 Web 点击读取一批推荐得到 20 条、排除 0 条，再点击首条“软件开发岗（西安）”读取详情并保存，站内正式详情显示正文且记录可见浏览交互。两次独立 HTTP 均 200，间隔至少 30 秒，无响应重放、翻页或刷新。

隔离库 `jobhunter-zhilian-production-live-AZrbxs` 保留，正式职位 `01a0baac-ada5-758f-86ed-695fc01ce98e`；jobs=1、sync_runs=0、missing_count=0、外键错误=0，五张相关持久表未见 at／rt／d。测试 Worker 和临时 Web 已停止，研究 CDP 回到空闲并清除上下文，连接继续保留。生产库未改动，未启动长期观察或晋级 supported。真实新建 WebSocket 的授权流程本轮未重复验证；其初始化、取消、超时和持有寿命由合成 CDP 定向测试覆盖。

48 项连接器／Worker 单元测试、3 项真实 CLI／生产装配隔离测试、7 项本地浏览器测试（含三种宽度 axe、CSRF、provider 隔离、幂等和保存导航）通过；相关类型、ESLint、Web 生产构建、严格 UI 审计、格式与文档检查通过。只跑定向检查，非全量门禁。真实多批翻页、其他详情格式与长期稳定性仍单独待验收。

2026-09-20 生产闭环计划：完成智联校园 CDP 模板初始化、生产 Worker 双平台装配、带 provider 的 CLI 和复用的 Web 面板；定向验证请求监听边界、连接复用、跨平台拒绝、CLI 到正式职位与 Web 投影。只用隔离库，不运行全量门禁，不恢复观察。T012 在验证完成后再勾选。

2026-09-20 原详情 parse_changed 已定位并修复：CC130384730J40845557616 的 HTTP／业务码均为 200，职位编号、标题、公司编号 CZ130384730 及名称均一致；原因是 939 字符正文包含 p／ol／li／br，首版仅识别 br。新增仅针对这些已观察无属性标签的结构校验和纯文本转换，有序列表按各自层级计数；未知标签、属性、实体、畸形闭合和空正文仍失败，身份校验不放宽。新增测试在旧实现下复现失败，修复后通过。

同一常驻授权连接取得上下文后，隔离线程重新加载修复包，对原失败职位再做一次真实 HTTP（无响应重放），经通用 Worker 保存到隔离正式库并统一查询成功，正文 714 字符，保留段落及编号；jobs=1、sync_runs=0、missing_count=0、外键错误=0、检查的持久表未见凭据。隔离库 `jobhunter-zhilian-detail-fixed-2y86td` 保留；旧失败任务不改写。两次在线详情请求分别用于诊断和修复验收，间隔超过 30 秒，无列表刷新、重连、生产库写入或全量门禁。32 项定向测试及相关类型、ESLint、格式、文档检查通过。此修复解决已知样本，不宣称任意 HTML 格式已支持。

2026-09-20 01:00 智联实时最小闭环通过：复用常驻已授权 CDP，未重新连接或弹出授权；正常切换一次 Java 推荐以获得当前模板。新增 ZhilianCampusHttpSession 固定端点／字段白名单、保留 URL 重复参数、不携带 Cookie 头，列表与详情分别实时 HTTP。第二次限量运行推荐 20 条、排除 0 条，从本次真实候选中选取此前已验证格式的“云计算研发实习生”，通过通用 Worker 的 connect → next → detail → disconnect 保存并查询到 1 条正式职位。该次运行有两次真实 HTTP，间隔 30000ms，响应重放为 0；jobs=1、observations=1、sync_runs=0、missing_count=0、外键错误=0，已检查的业务表未见 at／rt／d。隔离库 `jobhunter-zhilian-live-LEJRZU` 保留，生产库未修改。

同轮第一次运行的列表同样成功 20 条，但首条“Java 后端开发实习生”（CC130384730J40845557616）详情触发 parse_changed，未写职位；失败库 `jobhunter-zhilian-live-5JRzpF` 保留。尚未定位具体字段，不能称为该异常已修复，也不能把特定成功样本推广为全部详情可用。本轮总计两次列表、两次详情；未追加请求排查失败样本。24 项定向连接器／Worker／SQLite 测试、相关类型检查、ESLint 与格式检查通过。PLT-T014 完成，包含生产 CLI／Web 接入的 PLT-T012 仍未完成，supported 与长期稳定性不晋级。HTTP 会话断开与研究 CDP 生命周期分开，验证结束仅恢复空闲，不关闭用户要求保留的授权连接。

2026-09-20 完整请求对齐与单变量精简：同一授权 CDP 连接下，官网正常切换一次“热门推荐”（subjectType=1），捕获 POST 及响应，浏览器 statusCode=200／20 条。保留原始 URL 的重复查询参数、正文及应用请求头进行独立 HTTP；仅 HTTP/2 伪头、长度和压缩协商由 Node 传输层生成。随后每次只额外删除一个字段，至少间隔 30 秒，共三次独立请求：

| 对照                                               | HTTP／业务码 | 条数 | 与浏览器原批次的职位 ID 交集 |
| -------------------------------------------------- | ------------ | ---- | ---------------------------- |
| 完整应用请求基线                                   | 200／200     | 20   | 20                           |
| 基线仅删除 Cookie 头                               | 200／200     | 20   | 17                           |
| 在上一项基础上删除全部同名 x-zp-client-id 查询参数 | 200／200     | 20   | 16                           |

结论仅限本次热门推荐：Cookie 头和 URL 的 x-zp-client-id 可删；正文 d、at／rt、x-zp-at／x-zp-rt 请求头、x-zp-page-request-id 及其他字段仍保留，没有证明最小集合。推荐结果变化不能自动解释为筛选语义不变。另以本地只读比对确认正文 at／rt／d 与适用 Cookie 值一致，不新增上游请求。上轮 Java 意向推荐返回 210 的确切原因仍未定位；两轮场景不同，不能将本轮成功概括为前次失败原因已解决，尤其不能再认定遗漏 x-zp-client-id 必然导致 210。未改生产连接器、未入库、未恢复稳定性观察。

2026-09-20 继续验证：正常浏览器详情可见，官网公开详情前端确认 POST 协议。借用 at／rt／d 后，一次独立 HTTP 详情 statusCode=200，职位编号、标题、公司编号与正文有效；未执行 HTML 校验脚本。本轮推荐三次限量参数实验均返回业务码 210，间隔至少 30 秒，停止后不再访问平台。添加 resumeNumber、官网 actionid 与浏览器 UA 未恢复推荐；不能把失败归因于缺少某一个字段，更不能宣称字段最小化完成。

新增 ZhilianCampusSelectionSession 仅处理一个已显示卡片，详情成功后释放认证字段，保存动作消费同次内存事实而不重复 HTTP。使用本轮真实详情响应在内存重放进入此实现，再经真实通用 Worker／平台服务／SQLite 隔离库完成 connect → next → detail → disconnect：jobs=1、observations=1、sync_runs=0、missing_count=0、外键错误=0，检查任务／职位／修订／观察／连接表未见借用凭据，BOSS 工作集未受影响。隔离库保留供核验。该验证使用人工观察卡片与会话桥接、真实 HTTP 响应重放，不是生产 CDP 自动选取或端到端在线生产装配；PLT-T012 继续未完成。

本轮 14 项定向连接器／Worker 测试、相关类型检查、ESLint、格式与文档检查通过，未运行全量门禁。后续推荐复核须先比较完整已观察请求：前次成功重放保留了原始 URL，本轮重建端点未带官网 URL 中的 `x-zp-page-request-id`／`x-zp-client-id` 查询参数；这是尚未控制的差异，不足以认定失败原因。已达到本轮三次列表实验上限，不追加请求。

2026-09-20 智联最小实验：重试后单条授权连接成功；官网一次 Java 分类浏览提供请求结构，独立 HTTP 推荐一次返回 20 条。未携带 Cookie 头／浏览历史，但使用借用的登录令牌与推荐上下文，尚不能声称认证字段已最小化。其后一条匿名 HTML 详情返回 HTTP 200 脚本校验而非正文，停止在线请求，未执行校验脚本、未重试或绕过。官网详情数据接口尚未验证，PLT-T012 保持未完成；未注册生产智联适配器、未入库，也未恢复联合观察。详见 design.md 的“智联接入验证”。

2026-09-20 多平台基础收尾：用户明确后续目标为智联、前程无忧（51job）与猎聘，对标本地 BossHunter 的平台范围。本轮仅参考命名和职责拆分，独立实现 provider 绑定仓储、通用服务／任务工厂和 Web 结果投影，新增 0035 保留 BOSS 连接数据并扩展允许平台。四个平台相同外部职位／公司 ID、相同连接代次与相同幂等令牌仍隔离；跨平台任务写入拒绝，BOSS 失败、断开与重新初始化不影响其他实例。生产仍仅注册 BOSS 连接器，其他平台协议尚未实现。

交互通过 CSRF POST 在详情可见展示后同步记录；隐藏页／GET 不记录，失败可显式重试。后台保存不再伪装用户浏览；交互时间单调且不改变核验时间。清理状态读取真实安全投影，不暴露令牌，不扫描候选或请求平台。前端沿用既有布局与样式，修正新增说明在浅色背景上的对比度。40 项定向单元／SQLite／迁移／CLI 回归、5 项本地浏览器测试（含 axe）通过，类型、ESLint、依赖边界、Web 生产构建及严格 UI 静态审计通过。新增迁移只在隔离库运行，未运行全量门禁；联合稳定性观察仍暂停。

2026-09-20 用户调整观察计划：已暂停 `boss-24`，取消原四个执行窗口；后续改为目标平台接入完成后的 12 小时联合观察，第 3、6、9、12 小时各一轮，各平台一批列表加一条详情、请求间隔至少 30 秒，错峰执行。暂停不计为稳定性通过；保留隔离库证据，不追加 BOSS 请求。原专用 Worker 收到正常退出信号以释放测试连接，未关闭用户 Chrome 或退出账号。恢复前重新明确平台名单、授权连接和起点；当前明确的下一个平台是智联，其余平台名单待用户确认。

2026-09-19 23:25 当前进展（下文保留历史记录，不代表最新未完成项）：已补齐 `platform.retention` 应用端口、SQLite 引用保护、生产 Worker 小时维护装配及 CLI 预览／确认／停用／状态／交互记录。默认关闭，预览令牌五分钟有效且仅能消费一次，启用后至少等待一个策略间隔。关闭或未到期直接跳过；执行事务重新检查引用、每次最多删除 100 条，保留正式匹配、分析和业务引用，官网职位不受影响。真实平台保存 → 确定性匹配 → 幂等重放 → 清理保护已通过隔离测试。清理、平台、官网同步、匹配及 CLI/生产 Worker 共 28 项定向回归通过；没有新增上游请求或启用生产删除。

24 小时观察已单独安排：`boss-24` 沿用同一 Worker 与 generation=2 的已授权连接，2026-09-20 北京时间 05:06、11:06、17:06、23:06 开始四个半小时执行窗口，每轮最多一批推荐、一条详情且间隔至少 30 秒。初始化完成但首轮尚未执行；缺失窗口、连接变化或任一失败即停止，不补跑、不刷新、不重连。观察 Worker 保留启动时的代码，不为部署本轮清理变更重启。T002/T008 及 supported 晋级继续等待证据，不能把离线回归视为长期稳定通过。

清理操作：向运行中的 Worker 对应数据目录执行 `pnpm boss --data-root <目录> retention-preview --days 30 --interval-hours 6`，随后用 `result <任务ID>` 读取计数、不可撤销提示及令牌；确认策略后才执行 `retention-enable --confirmation-token <令牌>`。`retention-status` 查看状态，`retention-disable` 停用，`touch <职位ID>` 显式记录用户交互。上述操作均发布 Worker 任务，不在 CLI 中直接删除；预览不是备份。

用户确认恢复后的低频复核（2026-09-19 23:00）：仅只读查看现有推荐页，确认列表与详情可见，没有刷新或导航。真实生产装配在线测试通过，耗时约 34 秒：单条经用户授权的 CDP 连接、一批推荐与一条详情，两次独立 HTTP 至少间隔 30 秒；详情通过真实 Worker 写入隔离正式职位库并由统一查询读取，Web 应用状态恢复到已保存；无 sync_runs、无 missing_count 副作用，外键检查通过，显式断开成功。finally 关闭 Worker 并清理临时库，生产库未改动。此次不重试、不翻页，不抹除前次 37／36 证据，不证明长期稳定。测试默认已收缩为一批一详情，两批须显式选择。定向 ESLint、格式、文档及 diff 检查通过。

2026-09-19 Web 追加实现：提供 `/sources?channel=platform` 与 CSRF 保护的 `/api/platforms/boss`，Web 只发布任务；有限历史恢复当前代次最近一批、已保存职位链接、活动任务与安全错误。重复提交使用同一令牌确认，失败保留已成功结果，刷新不访问平台，字段错误聚焦并保留草稿。Worker 启动废弃旧代次、退出持久化断开状态。复用已有 UI 令牌与正式职位详情入口；T007 的在线整体验收仍未完成。

最新真实生产装配复核：新增 `apps/worker/test/boss-platform.online.test.ts`，通过 Web 同一应用发布入口驱动真实生产 Worker、BossCdpSessionProvider、HTTP 与 SQLite。用户批准唯一一次新连接后，连接成功，首批 HTTP 返回 `access_blocked (37)`；立即停止，没有翻页、详情请求或职位写入。finally 关闭 Worker/CDP 并移除该测试创建的临时库，未修改生产库。Chrome 原生窗口及标签页截图均呈空白；一次正常刷新后仍未显示职位，没有处理验证或自动重试。不能以早期桥接成功代替此次完整装配失败，也不能推断只是未登录。

本轮交付不包含平台自动保留清理，T006 仍未完成；网页明确提示尚未启用，没有提供无实现的清理按钮。

后续证据更正：用户反馈浏览器推荐页正常非空，故此前空白截图不足以判断用户浏览状态。随后只读核对同一 Chrome 标签页及原生窗口，均已出现“安全验证”，地址 `code=36`，文案为账号可能存在异常访问行为。此前进行过一次刷新，但无法确认验证由独立请求、刷新或其他因素触发；停止所有平台请求与刷新，未点击验证。不要把短暂空白或 HTTP 37 直接等同于未登录。

本轮定向验收：29 项单元／生产装配回归、17 项平台／迁移集成和 2 项 Web 浏览器测试通过；类型、定向 ESLint、生产构建、文档及依赖边界检查通过。严格 UI 审计零问题。在线 smoke 失败单独保留，不计入通过数量；未运行全量门禁。

最新结果：BOSS CLI/Worker 最小链路完成真实隔离 smoke。公司 ID 空字符串是本轮 parse_changed 原因，已改为排除并返回 skippedMissingCompanyId；未知类型仍报错。两批原始列表各 15 条，保留 10/11、排除 5/4、零重复；两条详情均由 Worker 任务正式入库并可查询，无官网同步和缺失计数副作用。共 4 次 HTTP，一条 CDP 连接；第一批为修复后内存重解析已收到的真实响应。临时库已清理，生产库未改动。27 项定向单元测试、13 项平台／官网同步集成测试通过；完整规格中的 Web、保留清理、长期稳定及智联仍未完成。

- [x] **PLT-T009**：按 ADR-0026 将 CDP 生命周期绑定到用户活动会话；验证单次连接跨动作复用、授权等待、错误冻结、断开／意外断线／Worker 退出清理。覆盖 PLT-004、PLT-006。

连接复用变更：25 项平台／应用单元测试与 1 项真实 SQLite/Worker 集成测试通过，类型和定向 ESLint 通过。模拟 CDP 验证超过 20 秒的人工等待、120 秒超时、HTTP 列表／详情共用一个 WebSocket、错误冻结及主动／意外关闭。此次没有触发真实 Chrome 新授权或上游 HTTP 请求；此前 parse_changed 仍待独立定位。

用户允许后的追加 smoke：CDP 成功，首批列表 parse_changed；第二次诊断连接再次遇到 Chrome 授权并超时。仅发出一批 HTTP，无详情及业务数据库写入。待定位响应字段及逐次授权体验，不晋级 supported。

定向验证：18 项平台协议／CDP 边界测试、69 项迁移／平台入库／官网同步／清理／任务回归通过；类型、ESLint、依赖边界、文档检查通过。没有运行全量门禁。

2026-09-19 后续实现：CLI → platform.boss → Worker → 正式 jobs/revisions/observations 链路已实现，并在临时 SQLite 上通过真实任务引擎验证。0034 迁移、公司外部身份、连接代次、取消／租约复核及官网来源隔离已实现；平台专用清理仍未完成，T004 保留未勾选直到整体查询／保留策略验收结束。浏览 UI、自动清理与 supported 晋级未交付。真实复核遇到 Chrome CDP 授权弹窗并超时，已请用户手动允许；外部 Chrome 列表与详情正常，未将连接失败记为未登录，未发出新 HTTP 请求。未迁移业务数据库、未提交推送。

2026-09-19：仅完成文档设计；所有实现、迁移和 supported 晋级均未执行。历史小实验结论见 design.md，不用成功列表掩盖第二条详情失败。没有运行全量门禁或修改业务数据库。

2026-09-19 实现进展：PLT-T002 已确认业务码 37 对应环境异常，本次仅一次 HTTP 首页请求即停止；用户确认浏览器正常。PLT-T003 部分完成两个包的契约、受限 HTTP、Cookie 匹配、内存候选参数与失败冻结，16 项离线测试通过；CDP 生产会话管理和连接代次尚未接入，因此不勾选该任务。PLT-T004 至 PLT-T008 未完成；生产推荐、详情、入库闭环不可用，不晋级 supported。无真实账号登出、批量采集或数据库变更。
