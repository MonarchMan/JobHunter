# 025 多模板简历制作与本地导出任务

> 状态：Implemented

- [x] RTE-T015 修复 Safari 沙箱事件回调，补充 CSP 防护、WebKit 交互与脚本阻断回归，并验证实际 Safari。

RTE-T015 验证：修复前两套模板在 WebKit 点击新增后块数为 0；修复后 Chromium／WebKit 共 4 项完整组件流程（含 axe、390px、保存恢复、脚本及外部资源 CSP 探针）通过。实际 Safari 验证 +1、输入失焦保存及重新载入恢复（版本 26→28）；测试内容通过内嵌浏览器清理至版本 29。实际 Safari 的其余操作由 WebKit 自动化覆盖，不声称全部已人工点击验证。

- [x] RTE-T014 修复浮动按钮、空白语言字段及结束时间的直接鼠标点击，覆盖两套模板的新增、输入和删除回归。

RTE-T014 验证：先复现后一个聚焦文本块拦截前一个块的按钮点击，再修复悬停层级与行内字段点击盒。两套模板浏览器组件回归覆盖 +1／+2／+3／删除、语言双字段、结束时间直接输入及保存重载，连同草稿应用服务共 3 项测试通过；329 项单测、格式、类型、构建、axe 和设计静态审计通过。未替代完整服务入口 E2E。

- [x] RTE-T013 修复文本块按钮鼠标／键盘操作，缩小为粉底浮动工具；新增左侧章节状态与确认删除，持久化空白已添加章节；统一列表退格和普通段落恢复并执行回归。

RTE-T013 验证：9 项模板单测、草稿应用服务回归，以及两套模板的真实 React／iframe 浏览器组件测试通过（使用 about:blank 和虚构草稿，不依赖运行中的 Web 服务）。覆盖按钮点击／Enter、章节确认增删、空白状态恢复、技能与项目退格后段落恢复、390px 与 axe。桌面／窄屏截图位于本地 `var/qa/`。完整服务入口 Playwright 流程仍单独跟踪，不由组件测试替代。

- [x] RTE-T012 将新增／删除入口落到每个文本块，采用悬停／聚焦浮动图标，支持当前块后插入及精确删除；空章节提供首块入口，导出不含控件。

- [x] RTE-T010 整体描述编辑、段落／列表恢复和 1–3 栏多段内容块；通过 Schema、渲染及路由回归。
- [ ] RTE-T011 执行新增浏览器回归：回车续写列表、退格取消圆点、多段单栏及重载恢复。

- [x] RTE-T001 建立共享模板包、公开投递 Schema、两套模板和渲染测试。
- [x] RTE-T002 建立草稿/导出表、Repository 端口与 SQLite 实现。
- [x] RTE-T003 建立草稿、头像、刷新、HTML 导出应用服务和 Web 契约。
- [x] RTE-T004 建立 PDF 任务 Handler、Worker Playwright 实现和临时文件清理。
- [x] RTE-T005 建立 API、在线简历入口、沉浸式制作页和响应式/无障碍状态。
- [x] RTE-T006 扩展删除影响预览及级联清理。
- [x] RTE-T007 完成单元、集成、Route、Worker、Playwright、axe 与视觉验证。
- [x] RTE-T008 居中顶部排版控件，按参考图调整姓名/求职方向层级，并将专业技能渲染为可编辑无序列表。
- [x] RTE-T009 增加同源模板效果预览，并修正制作页返回按钮与任务标题按钮的轻量悬停状态。
- [x] RTE-T010 收窄运行时导出目录的忽略规则，补齐创建、查询和交付导出的 Web API，并防止客户端把非 JSON 错误页直接解析为 JSON。
