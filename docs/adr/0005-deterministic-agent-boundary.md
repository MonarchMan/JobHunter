# ADR-0005：确定性管道优先与轻量 Agent

- 状态：Accepted
- 日期：2026-08-19

## 决策

采集、去重、状态转换、资格判断和基础评分使用确定性代码；LLM 仅执行结构化语义提取和建议。自建最小 Agent 运行时，不使用 LangChain。

## 原因

关键数据正确性必须可重放、可测试和可解释。通用编排框架会引入首期不需要的抽象，纯 LLM 决策则难以稳定验收。

## 后果

Agent 必须使用版本化 Prompt、Zod 输出、预算和缓存。模型故障时系统仍能同步、查询和执行基础评分。
